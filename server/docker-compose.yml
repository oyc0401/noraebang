services:
  adminer:
    image: adminer:5.4.2
    container_name: noraebang-adminer
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      ADMINER_DEFAULT_SERVER: host.docker.internal
